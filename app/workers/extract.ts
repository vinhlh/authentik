#!/usr/bin/env node

/**
 * Extraction worker removed.
 * We now use an external transcript/extraction service.
 */

console.error(
  "Extraction worker is disabled. YouTube/TikTok parsing was removed from this app; use the external transcript service."
)
process.exit(1)
